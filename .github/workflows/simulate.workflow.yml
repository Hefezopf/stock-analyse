name: Simulate

on:
  # Can be triggered with cURL
  #repository_dispatch:
    #types: [simulate] 

  # Can be triggered manually
  workflow_dispatch:
    inputs:
      symbolsParam:
        description: 'SYMBOLS - List of stock symbols like: "DBK BEI".'
        required: false
        default: 'DBK CRIN MOR FPE3 BC8 BEI'

# A workflow run is made up of one or more jobs that can run sequentially or in parallel
jobs:
  # This workflow contains a single job called "build"
  build:
    # The type of runner that the job will run on
    runs-on: ubuntu-latest

    # Steps represent a sequence of tasks that will be executed as part of the job
    steps:
      # Checks-out your repository under $GITHUB_WORKSPACE, so your job can access it
      - uses: actions/checkout@v2
            
      # Runs a set of commands using the runners shell
      - name: Run Simulate
        run: |
          echo "Start Simulate ..."

          export GPG_PASSPHRASE=${{secrets.GPG_PASSPHRASE}}

          #Depot+DAX+MDAX+SDAX+Eurostoxx+DOW (175). Not working properly, in marketstack.com (ENR SU1N AOX AI3A SEJ1 PPB PER):
          gpg --batch --yes --passphrase "$GPG_PASSPHRASE" config/own_symbols.txt.gpg 2>/dev/null
          ownSymbolsVar=$(sed 's/^/*/' config/own_symbols.txt | cut -f1 -d ' ' | tr '\n' ' ')
          rm -rf config/own_symbols.txt
          stockSymbolsVar=$(cat config/stock_symbols.txt)
          symbolsVar=$(echo $ownSymbolsVar $stockSymbolsVar)

          #symbolsVar='BEI HLE'



          #symbolsEnv=${{env.symbolsEnv}}


          symbolsInput="${{github.event.inputs.symbolsParam}}"

          
          symbolsVar=${symbolsInput:-$symbolsVar}
 

          #symbolsVar=${symbolsVar:-$symbolsEnv}          

          amountPerTradeVar=2000
          RSIBuyLevelVar=25
          StockSellLevelVar=91
          incrementPerTradeVar=1.1

          chmod +x ./simulate.sh

          sh ./simulate.sh "$symbolsVar" "$amountPerTradeVar" "$RSIBuyLevelVar" "$StockSellLevelVar" "$incrementPerTradeVar"

          git config user.email ${{secrets.MAIL_ADRESS}}
          git config user.name "Markus Hopf"
          git add out/_simulate.txt
          git commit -am "Github Result ${{github.event_name}} ${{github.event.action}}" --allow-empty 
          git push 

      - name: Send result via email
        uses: dawidd6/action-send-mail@v2
        with:
          server_address: smtp.ionos.de
          server_port: 465
          username: ${{secrets.MAIL_USERNAME}}
          password: ${{secrets.MAIL_PASSWORD}}          
          subject: Result ${{github.event_name}} ${{github.event.action}}
          # Read file contents as body:
          body: file://out/_result.html
          to: ${{secrets.MAIL_ADRESS}}
          from: Github stock-analyse # <githubÄhopf-...de>
          # Optional content type (defaults to text/plain):
          content_type: text/html          
          # Optional attachments:
          #attachments: out/_out.tar.gz
